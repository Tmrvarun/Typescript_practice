console.log('welcome to typescript');

//INSTALLATION:
//Download node js first
//Then download Visual studio
//Check the path of node js> it is already available
//Create folder in any drive > Load the folder in visual studio
//In VS create new folder > create a file in it 
// Download the tyscripty compiler by commant in cmd "npm install -g typescript"
//To execute code first convert code to js file by "tsc file path.ts" , then "node file path.ts"
// if during compilation, error occur "Running scripts is disable", then by command "Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned",is is removed
// install typescript executor by command "npm install -g tsx" if you want to skip the ts conversion to js